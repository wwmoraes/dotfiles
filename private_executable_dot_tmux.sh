#!/usr/bin/env sh

# command -V powerline >/dev/null 2>&1 || exit 0
# command -V powerline-daemon >/dev/null 2>&1 || exit 0
# command -V powerline-config >/dev/null 2>&1 || exit 0
# command -V powerline-render >/dev/null 2>&1 || exit 0

# powerline-daemon -q
# powerline-config tmux setup
