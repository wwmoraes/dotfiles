## filtering SOPS is a security risk when used with sops-nix as nix copies the
## file from the repo to the store, which is decrypted by the smudge filter
# secrets.yaml filter=sops

.github/workflows/integration.yml merge=write-file
flake.nix merge=write-flake
secrets.yaml diff=sops-yaml
